import { HighlighrCardDirective } from './highlighr-card.directive';

describe('HighlighrCardDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighrCardDirective();
    expect(directive).toBeTruthy();
  });
});
